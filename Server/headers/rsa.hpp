#pragma once

#include <bigint.hpp>

BigInt rsa_encrypt (BigInt msg) {
        // TODO : fix impl
        return (msg);
}

BigInt rsa_decrypt (BigInt msg, BigInt private_key) {
        // TODO : fix impl
        private_key;
        return (msg);
}