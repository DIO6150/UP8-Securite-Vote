#pragma once

#include <string>

using BigInt = uint64_t;

const bool BINToString (const BigInt bing, std::string & str) {
        // TODO : fix impl
        str = "STR";
        return (true);
}

/*

*/
bool StringToBINT (const std::string & str, BigInt & bing) {
        // TODO : fix impl
        bing = 123456789;
        return (true);
}